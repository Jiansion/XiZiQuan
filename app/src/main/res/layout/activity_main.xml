<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mDrawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.conch.xiziquan.ui.activity.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <FrameLayout
            android:id="@+id/mFrameLayout"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <RadioGroup
            android:id="@+id/radioGroup"
            android:layout_width="match_parent"
            android:layout_height="?actionBarSize"
            android:background="#80151515"
            android:orientation="horizontal">

            <RadioButton
                android:id="@+id/rbIndex"
                style="@style/BottomTabStyle"
                android:drawableTop="@drawable/btn_tab_index_selector"
                android:text="首页" />

            <RadioButton
                android:id="@+id/rbForum"
                style="@style/BottomTabStyle"
                android:drawableTop="@drawable/btn_tab_forum_selector"
                android:text="论坛" />

            <RadioButton
                android:id="@+id/rbActivity"
                style="@style/BottomTabStyle"
                android:drawableTop="@drawable/btn_tab_activity_selector"
                android:text="活动" />

            <RadioButton
                android:id="@+id/rbUser"
                style="@style/BottomTabStyle"
                android:drawableTop="@drawable/btn_tab_user_selector"
                android:text="用户" />
        </RadioGroup>
    </LinearLayout>


</android.support.v4.widget.DrawerLayout>
